# Gradle configuration
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true

# Project configuration
group=com.diyawanna
version=1.0.0

# Maven Central publishing (set these via environment variables or command line)
# ossrhUsername=your_username
# ossrhPassword=your_password

# Signing configuration (set these via environment variables or command line)
# signingKey=your_signing_key
# signingPassword=your_signing_password

# Build configuration
kotlin.code.style=official